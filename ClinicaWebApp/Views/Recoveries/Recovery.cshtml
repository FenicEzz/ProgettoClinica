@model ClinicaWebApp.Models.RecoveryViewModel

<h1>Ricovero per ID: @Model.PetId</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Recovery">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input asp-for="PetId" class="form-control" type="hidden" value="@Model.PetId" />
            <input asp-for="RecoveryDate" class="form-control" type="hidden" value="@DateTime.Now" />
            <div class="form-group">
                <label asp-for="UrlPicture" class="control-label"></label>
                <input asp-for="UrlPicture" class="form-control" />
                <span asp-validation-for="UrlPicture" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-success my-3" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="btn btn-primary">Back to List</a>
</div>



@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}